import { atom } from "recoil";

export const videoLinkState = atom({
    key: 'videoLinkState',
    default: '',
});

export const gunLinkState = atom({
    key: 'gunLinkState',
    default: '',
});

export const btnColorState = atom({
    key: 'btnColorState',
    default: '',
});